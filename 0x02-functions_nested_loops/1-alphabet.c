#include "main.h"

/**
 * main - print_alphabet print all alphabet in lowercase
 *
 * Return: 0 (success)
 */

void (print_alphabet(void))
{
	char letter;

	for (letter = 'a'; letter++)
		_putchar(letter);

	_putchar('\n');
return (0);
}
